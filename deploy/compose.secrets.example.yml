# Legacy / secrets-focused example.
# Prefer the full stack at repo root:
#
#   docker compose --env-file env/.env up --build
#
# This file now mirrors that stack (Postgres + Redis + Neo4j + MCP + API)
# for callers that still use deploy/*.yml.

include:
  - path: ../docker-compose.yml
